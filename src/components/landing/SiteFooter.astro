<!-- BEGIN: Footer -->
<footer
    data-animate-section
    class="w-full bg-[#0A0A0A] text-white pt-24 md:pt-32 pb-12 overflow-hidden"
>
    <div class="max-w-[1440px] mx-auto px-6">
        
        {/* Top Section: Branding & Newsletter */}
        <div class="grid grid-cols-1 lg:grid-cols-12 gap-16 mb-24 items-start">
            <div class="lg:col-span-5">
                 <h2 class="text-[10px] uppercase tracking-[0.5em] text-gray-500 font-black mb-8 block">Newsletter</h2>
                 <p class="text-xl md:text-2xl font-bold uppercase tracking-tighter mb-10 max-w-sm">
                    Join the archive for <span class="text-gray-500 italic font-light">exclusive</span> access.
                 </p>
                 <form class="relative group max-w-md">
                    <input 
                        type="email" 
                        placeholder="EMAIL ADDRESS" 
                        class="w-full bg-transparent border-b border-white/20 py-4 text-[11px] uppercase tracking-[0.2em] outline-none focus:border-white transition-colors placeholder:text-gray-700"
                    />
                    <button class="absolute right-0 bottom-4 text-[10px] uppercase tracking-[0.3em] font-black hover:text-gray-400 transition-colors">
                        Subscribe →
                    </button>
                 </form>
            </div>
            
            <div class="lg:col-span-7 grid grid-cols-2 md:grid-cols-3 gap-12 text-[10px] uppercase tracking-[0.25em] font-bold text-gray-500">
                <div class="flex flex-col space-y-4">
                    <span class="text-white font-black mb-4 tracking-[0.4em]">Company</span>
                    <a class="hover:text-white transition-colors" href="/about">About Us</a>
                    <a class="hover:text-white transition-colors" href="/collections">Collections</a>
                    <a class="hover:text-white transition-colors" href="/shop">Archive</a>
                    <a class="hover:text-white transition-colors" href="/about">Sustainability</a>
                </div>
                <div class="flex flex-col space-y-4">
                    <span class="text-white font-black mb-4 tracking-[0.4em]">Support</span>
                    <a class="hover:text-white transition-colors" href="#">Shipping</a>
                    <a class="hover:text-white transition-colors" href="#">Returns</a>
                    <a class="hover:text-white transition-colors" href="#">Sizing</a>
                    <a class="hover:text-white transition-colors" href="#">FAQ</a>
                </div>
                <div class="flex flex-col space-y-4">
                    <span class="text-white font-black mb-4 tracking-[0.4em]">Social</span>
                    <a class="hover:text-white transition-colors" href="#">Instagram</a>
                    <a class="hover:text-white transition-colors" href="#">Telegram</a>
                    <a class="hover:text-white transition-colors" href="#">Facebook</a>
                    <a class="hover:text-white transition-colors" href="#">Pinterest</a>
                </div>
            </div>
        </div>

        {/* Massive Brand Name with Reveal Animation */}
        <div class="border-t border-white/5 pt-12 md:pt-20">
            <div class="overflow-hidden">
                <a
                    href="/"
                    class="gsap-footer-brand block font-display text-[clamp(4rem,18vw,280px)] leading-[0.7] font-bold uppercase tracking-tighter text-center will-change-transform text-white"
                    style={{ letterSpacing: "-0.05em" }}
                >
                    Triostep
                </a>
            </div>
        </div>

        {/* Bottom Bar */}
        <div class="mt-20 flex flex-col md:flex-row justify-between items-center gap-8 border-t border-white/5 pt-12 text-[9px] uppercase tracking-[0.3em] font-black text-gray-600">
            <div class="flex gap-8">
                <span>© 2024 Triostepdkhyber</span>
                <a href="#" class="hover:text-white transition-colors">Privacy</a>
                <a href="#" class="hover:text-white transition-colors">Terms</a>
            </div>
            
            <div class="flex items-center gap-6">
                <span class="text-gray-800 italic">Designed by Triostep Lab</span>
                <div class="flex gap-2 opacity-30 grayscale">
                    {/* Placeholder for payment logos */}
                    <div class="w-8 h-5 bg-white/20"></div>
                    <div class="w-8 h-5 bg-white/20"></div>
                    <div class="w-8 h-5 bg-white/20"></div>
                </div>
            </div>
        </div>
    </div>
</footer>

<script>
    import gsap from 'gsap';
    import { ScrollTrigger } from 'gsap/ScrollTrigger';
    gsap.registerPlugin(ScrollTrigger);

    function initFooter() {
        gsap.from(".gsap-footer-brand", {
            yPercent: 100,
            duration: 1.5,
            ease: "power4.out",
            scrollTrigger: {
                trigger: ".gsap-footer-brand",
                start: "top 95%",
            }
        });
    }

    document.addEventListener('DOMContentLoaded', initFooter);
    document.addEventListener('astro:page-load', initFooter);
</script>
<!-- END: Footer -->
